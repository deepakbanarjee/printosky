requests==2.33.1
supabase==2.15.0
python-dotenv==1.2.2
# v3 engine deps (Claude Vision hybrid PDF/DOCX -> DOCX). These were
# previously pulled from root requirements.txt under modern config, but
# the legacy 'builds' array installs ONLY from api/requirements.txt, so
# they MUST be declared here. Keep in sync with root requirements.txt.
Pillow>=10.0.0
python-docx==1.1.2
pdfplumber==0.11.4
PyMuPDF==1.26.7
anthropic>=0.51.0
# Inngest: durable background job runner for v4 (api/inngest.py).
# Used as a separate Vercel serverless function exposing /api/inngest.
# Without this, the format-job-create endpoint would have no async worker
# and v4 documents would stay in pending state forever.
inngest>=0.4
# Flask is required only by api/inngest.py for the Inngest framework
# adapter (inngest.flask.serve). api/index.py keeps using BaseHTTPRequestHandler.
flask>=3.0
