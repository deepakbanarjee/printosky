# Printosky Tasks — 2026-04-13

## 🔴 Technical Issues (from startup log)

- [ ] **Fix Supabase `epson_jobs` upsert** — add unique constraint on `job_id` (or `printer_job_id`) in Supabase SQL Editor
  - Error: `42P10 — there is no unique or exclusion constraint matching the ON CONFLICT specification`
  - Fix: `ALTER TABLE epson_jobs ADD CONSTRAINT epson_jobs_job_id_key UNIQUE (job_id);`
- [x] **Fix Epson SNMP** — IP changed to 192.168.55.202, SNMP confirmed working ✓
  - All source files updated: printer_poller.py, epson_jobs_fetcher.py, print_server.py, watcher.py etc.
- [ ] **Set Konica job export URL** — auto-discovery failed for all 8 URL patterns
  - Open http://192.168.55.110 → job log → CSV export → inspect URL in DevTools (F12 → Network)
  - Set `KONICA_JOB_EXPORT_URL` in `konica_jobs_fetcher.py`
- [ ] **Fix Konica supply level parsing** — Toner Black, Drum Black all return `None`
  - Root cause: XML parsing not extracting values from bizhub response
  - File: `printer_poller.py`
- [ ] **Resolve 93 pending jobs** — oldest from 2026-03-12 (777h ago)
  - Use `done <JOB_ID> <AMOUNT> <MODE>` for jobs that were completed
  - Or bulk-close old jobs that are no longer actionable

---

## 🏪 Store Physical Tasks

### WhatsApp
- [ ] Install WhatsApp Business App on store phone (SIM: 9495706405)
- [ ] Verify coexistence — test message should appear on phone AND trigger Vercel webhook

### Security (CRITICAL — before going fully live)
- [ ] Add `META_APP_SECRET` to Vercel env vars (from developers.facebook.com → App ID 922193850568204 → Settings → Basic)
  - Then redeploy: `vercel --prod` from `C:\PY\printosky`
- [ ] Rotate `META_WEBHOOK_VERIFY_TOKEN` — generate new token, update Vercel + Meta dashboard
- [ ] Change Epson web panel default password (http://192.168.55.202 → admin/admin → Security → Change Password)
  - Update new password in `epson_jobs_fetcher.py`
- [ ] Audit `make_arch_pdf.py` for hardcoded passwords → rotate + move to `.env`

### Store PC Verification
- [ ] Confirm `printer_poller.py` reaches Konica at 192.168.55.110 (run standalone to check)
- [ ] Confirm `epson_jobs_fetcher.py` reaches http://192.168.55.202
- [ ] End-to-end test: send PDF via WhatsApp → confirm download to `C:\Printosky\Jobs\Incoming\` within 60s
- [ ] Confirm `file_url` column exists in `jobs.db` (SCHEMA_v3 migration)

### Razorpay
- [ ] Set Razorpay webhook URL to `https://printosky.vercel.app/webhook/razorpay`
  - Events: `payment.captured` + `payment_link.paid`
- [ ] Stop CloudFlare tunnel on store PC (no longer needed)

### Staff PINs
- [ ] Run `SCHEMA_v12_staff_cloud.sql` in Supabase SQL Editor (adds staff table)
- [ ] Add `ADMIN_PASSWORD_HASH` to Vercel env vars
- [ ] Reset all staff PINs via API: priya, revana, bini, anu, deepak

### Admin Dashboard
- [ ] Open admin → confirm Conversations tab loads
- [ ] Send test WhatsApp message → verify it appears in Conversations tab

---

## Notes
- Deploy command: `vercel --prod` from `C:\PY\printosky`
- Supabase project: mlhuwlnwwwxdnqafelko
- Konica: http://192.168.55.110 | Epson: http://192.168.55.202
- Store PC print server: http://localhost:3005
